<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bbbond.moduleOreo">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity
            android:name="com.bbbond.moduleOreo.home.OreoMainActivity"
            android:label="@string/oreo_module_name" />
        <activity
            android:name="com.bbbond.moduleOreo.pip.OreoPIPActivity"
            android:label="@string/oreo_pip_mode"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            tools:targetApi="n" />
        <activity
            android:name="com.bbbond.moduleOreo.notifications.OreoNotificationActivity"
            android:launchMode="singleTask"
            android:label="@string/oreo_notification" />

        <service android:name=".notifications.MyNotificationListenerService"
            android:label="@string/notification_listener_service"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>